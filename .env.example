# ===========================================
# UX Research Tool - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# -------------------------------------------
# Authentication (Required)
# -------------------------------------------

# Your app URL (no trailing slash)
NEXTAUTH_URL=http://localhost:3000

# Secret for JWT encryption (min 32 characters)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-at-least-32-characters-long

# -------------------------------------------
# Admin Credentials (Optional)
# -------------------------------------------
# Used when running: npm run db:seed

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# -------------------------------------------
# Database (Production only)
# -------------------------------------------
# For local development, SQLite is used automatically (prisma/dev.db)
# For production, set your database URL here

# PostgreSQL (Neon, Supabase, Railway):
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# MySQL (PlanetScale):
# DATABASE_URL=mysql://user:password@host:3306/database?sslaccept=strict

# Turso (LibSQL):
# TURSO_DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=your-auth-token
